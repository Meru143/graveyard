# graveyard

`graveyard` is a compiled Rust binary that scans polyglot repositories for dead code and ranks findings with a git-history-weighted confidence score.

## Installation

Installation instructions will be added as the distribution pipeline is wired up.
